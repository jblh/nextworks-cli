# Doctor Installed State Recorded Kits Spec

## Overview

Report recorded install state from the nextworks tracker.

## Requirements

- The doctor command must read recorded installs from `.nextworks/config.json` using existing installation-tracker utilities where possible.
- The doctor command must include recorded installed kits in the doctor result.
