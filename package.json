{
  "name": "nextworks-cli",
  "private": false,
  "workspaces": [
    "cli",
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "clean": "npm --workspaces --if-present run clean",
    "sync": "node scripts/sync-kit-to-packages.mjs",
    "watch": "node scripts/watch-dev.mjs",
    "continue": "node .continue/scripts/continue.mjs",
    "continue:new": "node .continue/scripts/continue.mjs --since-start",
    "continue:day": "node .continue/scripts/continue.mjs --day"
  },
  "dependencies": {
    "chokidar": "^5.0.0"
  }
}
